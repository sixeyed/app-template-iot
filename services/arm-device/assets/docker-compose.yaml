version: "3.6"

configs:
  device-ip:
    file: {{ .Name }}/configs/device-ip

secrets:
  device-docker-ca:
    file: {{ .Name }}/certs/ca.pem
  device-docker-cert:
    file: {{ .Name }}/certs/cert.pem
  device-docker-key:
    file: {{ .Name }}/certs/key.pem