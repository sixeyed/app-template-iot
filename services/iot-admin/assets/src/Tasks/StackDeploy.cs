using System;
using System.Threading.Tasks;

namespace IotStarterKit
{
    public class StackDeploy
    {
        /*
        host needs docker cli
        call with --host etc. settings
        bundle stacks
        add versioning?
         */
    }
}