{
  "Timer": {
    "DelayMilliseconds": 1000,
    "PeriodMilliseconds": 3000
  },
  "TelemetryService": {
    "Url": "{{range .Services}}{{if eq "service-demo" .ID}}http://{{.Name }}/readings{{end}}{{end}}"
  }
}
